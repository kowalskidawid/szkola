@model IEnumerable<Szkoła.Models.StudentsModel>

@foreach( var student in Model )
{
    <div class="row">
        <label class="mx-auto my-1">
            <input type = "checkbox" value="@student._id" />
            @student.FirstName @student.SecondName @student.LastName
         </label>
    </div>
}